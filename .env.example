# PostgreSQL connection string
DATABASE_URL="postgresql://USER:PASSWORD@localhost:5432/lexika?schema=public"

# NextAuth
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="replace-with-a-long-random-string"

# Netlify production URL (set this in the Netlify dashboard)
# NEXTAUTH_URL="https://your-site-name.netlify.app"

# Used only by prisma/seed.ts to create the first admin account
SEED_ADMIN_EMAIL="admin@lexika.app"
SEED_ADMIN_PASSWORD="ChangeMe123!"
